# 2026-05-05 EBSmoothr 0.2.1 package check

## Summary

- Bumped EBSmoothr from `0.2.0` to `0.2.1`.
- Added `NEWS.md` entries for the Matérn sparse selected-inverse variance path,
  public TMB-or-error Laplace backend semantics, and strict
  `matern_n_starts` handling.
- This check is intended to stage the accumulated 0.2.1 package update while
  leaving unrelated runtime benchmark artifacts unstaged.

## Validation

- `roxygen2::roxygenize("EBSmoothr")` completed.
- `testthat::test_file("EBSmoothr/tests/testthat/test-softplus-moments.R")`
  passed with 50 assertions.
- `testthat::test_file("EBSmoothr/tests/testthat/test-lgp.R")` passed with
  49 assertions.
- `testthat::test_file("EBSmoothr/tests/testthat/test-eb-smoother.R")` passed
  with 121 assertions.
- Custom Matérn targeted assertions passed for:
  - public `laplace` / `laplace_fisher` erroring instead of using the internal
    R reference path when TMB is unavailable,
  - explicit internal `laplace_r` still working,
  - strict known-noise and learned-noise `matern_n_starts` behavior,
  - selected-inverse posterior variance matching dense references for diagonal,
    covered sparse, and uncovered fallback designs.
- `R CMD build --no-build-vignettes EBSmoothr` built
  `EBSmoothr_0.2.1.tar.gz`.
- `R CMD check --no-manual --no-tests --no-examples EBSmoothr_0.2.1.tar.gz`
  completed with `Status: 1 NOTE`.

## Remaining check note

- The only check note was the pre-existing R CMD check diagnostic for
  `unlockBinding(default_name, ns)` in `R/02_Matern.R`.
